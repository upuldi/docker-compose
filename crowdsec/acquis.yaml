filenames:
  - /var/log/npm/*.log
labels:
  type: nginx-proxy-manager

---
filenames:
 - /var/log/vw/access.log
labels:
  type: Vaultwarden

---
source: docker
container_name:
 - authentik
labels:
  type: authentik